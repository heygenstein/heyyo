#include <iostream>
using namespace std;
int main()
{	
	int i,enBuyuk;
	int sayilar[5] = { 15,76,50,80,26 };
	enBuyuk = sayilar[0];
		for(i=0;i<=4;i++)
		{
			if (sayilar[i] > enBuyuk)
			{
				enBuyuk = sayilar[i];
			}
		}
		cout << "En buyuk sayimiz:" << enBuyuk;
		return 0;
}
